name: on-main
concurrency:
  group: ${{ github.workflow }}

on:
  push:
    branches: [main]

permissions:
  contents: read
  id-token: write

env:
  TERRAFORM_VERSION: 1.6.1

jobs:
  # businessApproval:
  #   name: Business Approval
  #   runs-on: ubuntu-latest
  #   environment: test
  #   steps:
  #     - run: echo Running business approval...

  test:
    name: test
    runs-on: ubuntu-latest
    # needs: businessApproval
    environment: test
    steps:
      - run: |
          import json
          import os
          import subprocess

          SECRETS = json.loads(os.environ['ALL_SECRETS'])

          for k, v in SECRETS.items():
            cmd = 'echo {}={} >> $GITHUB_ENV'.format(k, v)
            proc = subprocess.Popen(cmd, shell=True, stdout=subprocess.PIPE)
        shell: python
        env:
          ALL_SECRETS: ${{ toJSON(secrets) }}

      - run: echo Test

